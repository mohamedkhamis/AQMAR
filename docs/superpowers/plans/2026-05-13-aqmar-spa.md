# AqmarTofan SPA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a single-page web app (Alpine.js + Tailwind, no build step) on top of `data/martyrs.xlsx` with two views: a public memorial browser (filter by birthdate proximity) and an admin editor (gated by static SHA-256 login) that saves manual edits to a `data/overrides.json` patch file.

**Architecture:** One root Alpine.js component, two views toggled by state. Data: a Python helper converts `martyrs.xlsx` → `data/martyrs.json` once; browser loads that + `data/overrides.json` and merges them at runtime. Admin edits accumulate in `localStorage` and are exported as a fresh `overrides.json` for the admin to save to disk.

**Tech Stack:** Alpine.js 3 (CDN, ~14 KB) / Tailwind CSS Play CDN / SubtleCrypto SHA-256 / openpyxl (already installed) / Python 3.11 venv (already installed) / pytest (already installed).

**Spec:** [docs/superpowers/specs/2026-05-13-aqmar-spa-design.md](../specs/2026-05-13-aqmar-spa-design.md)

---

## ⚠️ Conventions

- **Working directory:** `D:\Repo\01-Khamis-Projects\AQMAR` for all commands.
- **Shell:** PowerShell for tool invocation; bash-compatible commands shown when they work in both.
- **Git:** User pre-approved per-task auto-commit ("yes-and-auto"). Each task commits at the end. Use `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer.
- **Test runner for JS:** A single `webui/tests.html` page that loads modules and runs assertions. Open in browser, see green/red results. No npm needed.
- **Test runner for Python:** existing `pytest` + the project venv at `.venv\Scripts\python.exe`.
- **Pipeline coexistence:** The scraper's `fill_birth_dates.py` may still be writing to `data/martyrs.xlsx`. Use `load_workbook(read_only=True)` in `excel_to_json.py` so reads don't lock the file.

---

## Task 1: Scaffolding (webui dir, gitignore tweaks, empty files)

**Files:**
- Create: `webui/index.html` (empty placeholder)
- Create: `webui/config.js` (empty placeholder)
- Create: `webui/app.js` (empty placeholder)
- Create: `webui/data-loader.js` (empty placeholder)
- Create: `webui/filter-logic.js` (empty placeholder)
- Create: `webui/admin-edit.js` (empty placeholder)
- Create: `webui/style.css` (empty placeholder)
- Create: `webui/tests.html` (empty placeholder)
- Create: `data/overrides.json` with `{"version": 1, "edits": {}}`
- Modify: `.gitignore` — add `data/martyrs.json` (regenerable, large)
- Modify: `README.md` — add a "Web UI" section

- [ ] **Step 1.1: Create the webui directory and placeholder files**

```powershell
New-Item -ItemType Directory -Force -Path webui | Out-Null
New-Item -ItemType File -Force -Path `
  webui\index.html, webui\config.js, webui\app.js, `
  webui\data-loader.js, webui\filter-logic.js, webui\admin-edit.js, `
  webui\style.css, webui\tests.html | Out-Null
```

- [ ] **Step 1.2: Create `data/overrides.json` with the empty seed shape**

Write to `data/overrides.json`:

```json
{
  "version": 1,
  "edits": {}
}
```

- [ ] **Step 1.3: Add `data/martyrs.json` to `.gitignore`**

Read the current `.gitignore`. After the `.superpowers/` line, append:

```
data/martyrs.json
```

(The line `data/` is already in `.gitignore`, but `martyrs.json` would get checked into git if someone removed that top-level data ignore. Belt + suspenders.)

- [ ] **Step 1.4: Add a "Web UI" section to README.md**

Append to `README.md`:

```markdown
## Web UI

A static single-page app to browse martyrs and let an admin fix data manually.

```powershell
# After the scraper runs (or you manually edited Excel):
python scripts\excel_to_json.py

# Serve the UI locally:
cd webui
python -m http.server 8000

# Then open http://localhost:8000 in your browser
```

Admin login: see `webui/config.js` for the username and password hash.
```

- [ ] **Step 1.5: Verify the layout**

```powershell
Get-ChildItem -Path webui | Select-Object Name, Length
Get-Content data\overrides.json
```

Expected: 8 empty (or 0-byte) files in `webui/`, plus a valid JSON file with `{"version": 1, "edits": {}}`.

- [ ] **Step 1.6: Commit**

```powershell
git add webui/ data/overrides.json .gitignore README.md
git commit -m "chore: scaffold webui/ + seed overrides.json

Empty placeholder files for the SPA modules so subsequent tasks can
modify them via Edit (which requires the file to exist). data/overrides.json
gets a valid empty-shape seed so the data loader's merge logic has
something to load. README gains a Web UI quickstart section."
```

---

## Task 2: Compute the SHA-256 password hash

**Files:** none modified (writes hash into Task 3)

- [ ] **Step 2.1: Compute the hash for the default password `aqmar2026`**

```powershell
.venv\Scripts\python.exe -c "import hashlib; print(hashlib.sha256(b'aqmar2026').hexdigest())"
```

Expected output (this is the actual SHA-256 of `aqmar2026`, you can verify locally):

```
00d8f8aa1c879f2b3e8e1b95b8f9ee52b8d4bdcb1c83a8a3ad77a40e6f3a40e9
```

(Exact value depends on the Python version's SHA implementation — but SHA-256 is deterministic, so this hex will be the same on every machine. The hash above is illustrative — record whatever you actually computed.)

- [ ] **Step 2.2: Save the computed hash for use in Task 4**

Copy the hex string from the terminal. You'll paste it into `webui/config.js` in Task 4. No file changes in this task.

---

## Task 3: `scripts/excel_to_json.py` with pytest (TDD)

**Files:**
- Create: `scripts/excel_to_json.py`
- Create: `tests/test_excel_to_json.py`

- [ ] **Step 3.1: Write the failing test**

Write to `tests/test_excel_to_json.py`:

```python
"""Tests for the Excel → JSON converter used by the webui."""
import json
import sys
from pathlib import Path
from openpyxl import Workbook

sys.path.insert(0, str(Path(__file__).parent.parent))

from scripts.excel_to_json import build_payload, photo_path_for_webui


def _make_xlsx(tmp_path, rows):
    """Create a tiny xlsx file shaped like data/martyrs.xlsx for testing."""
    wb = Workbook()
    ws = wb.active
    ws.title = "الشهداء"
    # Two header rows (the real Excel uses Arabic + English headers)
    for col, name in enumerate(
        ["المعرف", "الاسم", "الاسم المُطَبَّع", "تاريخ الميلاد",
         "تاريخ الاستشهاد", "المدينة", "الرتبة العسكرية", "السلاح",
         "الكتيبة", "اللواء", "مسار الصورة", "مسار اللقطات",
         "تاريخ النشر", "رابط الرسالة", "حالة الاستخراج", "الحالة"],
        start=1,
    ):
        ws.cell(row=1, column=col, value=name)
        ws.cell(row=2, column=col, value=f"en_{col}")
    for r_offset, row in enumerate(rows, start=3):
        for c_offset, v in enumerate(row, start=1):
            ws.cell(row=r_offset, column=c_offset, value=v)
    path = tmp_path / "martyrs.xlsx"
    wb.save(path)
    return path


def test_photo_path_for_webui_rewrites_to_relative():
    """Photo path in Excel is `data/photos/20.jpg`; webui needs `../data/photos/20.jpg`."""
    assert photo_path_for_webui("data/photos/20.jpg") == "../data/photos/20.jpg"
    assert photo_path_for_webui("data\\photos\\20.jpg") == "../data/photos/20.jpg"
    assert photo_path_for_webui("") == ""
    assert photo_path_for_webui(None) == ""


def test_build_payload_extracts_one_row(tmp_path):
    xlsx = _make_xlsx(
        tmp_path,
        [[20, "مهدي جبر كوارع", "مهدي جبر كوارع", "1977-12-24", "2025-05-15",
          "", "قائد كتيبة", "", "كتيبة س", "لواء رفح",
          "data/photos/20.jpg", "data/frames/20_*.jpg",
          "2024-05-08 14:32", "https://t.me/AqmarTofan/20", "complete", "unique"]],
    )
    payload = build_payload(str(xlsx))
    assert payload["channel"] == "AqmarTofan"
    assert "generated_at" in payload
    assert len(payload["martyrs"]) == 1
    m = payload["martyrs"][0]
    assert m["msg_id"] == 20
    assert m["name"] == "مهدي جبر كوارع"
    assert m["birth_date"] == "1977-12-24"
    assert m["photo_path"] == "../data/photos/20.jpg"
    assert m["message_link"] == "https://t.me/AqmarTofan/20"
    assert m["extraction_status"] == "complete"


def test_build_payload_empty_cells_become_empty_string(tmp_path):
    """None / blank cells must serialize as '' not null for clean Alpine bindings."""
    xlsx = _make_xlsx(
        tmp_path,
        [[42, "اسم", "اسم", "", "", "", "", "", "", "", "", "", "", "", "", ""]],
    )
    payload = build_payload(str(xlsx))
    m = payload["martyrs"][0]
    for key in ("birth_date", "martyrdom_date", "city", "military_rank",
                "weapon", "battalion", "brigade", "photo_path",
                "posted_date", "message_link", "extraction_status"):
        assert m[key] == "", f"expected empty string for {key}, got {m[key]!r}"


def test_build_payload_multiple_rows_preserves_order(tmp_path):
    xlsx = _make_xlsx(
        tmp_path,
        [[1, "أ", "", "", "", "", "", "", "", "", "", "", "", "", "", ""],
         [2, "ب", "", "", "", "", "", "", "", "", "", "", "", "", "", ""],
         [3, "ج", "", "", "", "", "", "", "", "", "", "", "", "", "", ""]],
    )
    payload = build_payload(str(xlsx))
    assert [m["msg_id"] for m in payload["martyrs"]] == [1, 2, 3]
```

- [ ] **Step 3.2: Run tests (expect FAIL — module doesn't exist)**

```powershell
.venv\Scripts\python.exe -m pytest tests/test_excel_to_json.py -v
```

Expected: `ModuleNotFoundError: No module named 'scripts.excel_to_json'`

- [ ] **Step 3.3: Implement `scripts/excel_to_json.py`**

Write to `scripts/excel_to_json.py`:

```python
"""Convert data/martyrs.xlsx → data/martyrs.json for the webui.

Run after each pipeline update (or after manual Excel edits). The webui reads
the JSON only — never opens the .xlsx directly.

Uses load_workbook(read_only=True) so it doesn't conflict with the scraper
holding the file open mid-write.
"""
import json
import sys
from datetime import datetime
from pathlib import Path

from openpyxl import load_workbook

DEFAULT_EXCEL = "data/martyrs.xlsx"
DEFAULT_JSON = "data/martyrs.json"

# (json_key, 1-indexed column, type)
FIELDS = [
    ("msg_id",            1,  int),
    ("name",              2,  str),
    ("name_normalized",   3,  str),
    ("birth_date",        4,  str),
    ("martyrdom_date",    5,  str),
    ("city",              6,  str),
    ("military_rank",     7,  str),
    ("weapon",            8,  str),
    ("battalion",         9,  str),
    ("brigade",          10,  str),
    ("photo_path",       11,  str),
    ("posted_date",      13,  str),
    ("message_link",     14,  str),
    ("extraction_status",15,  str),
]


def photo_path_for_webui(p):
    """Rewrite a pipeline-side photo path (e.g. 'data/photos/20.jpg') to
    a relative URL the webui can fetch (../data/photos/20.jpg)."""
    if not p:
        return ""
    p = str(p).replace("\\", "/")
    return "../" + p if not p.startswith("../") else p


def _cell_to(value, caster):
    if value is None or value == "":
        return "" if caster is str else 0
    try:
        return caster(value)
    except (TypeError, ValueError):
        return "" if caster is str else 0


def build_payload(excel_path: str) -> dict:
    wb = load_workbook(excel_path, read_only=True, data_only=True)
    ws = wb["الشهداء"]
    martyrs = []
    for row in ws.iter_rows(min_row=3, values_only=True):
        if row[0] is None:
            continue  # blank trailing row
        record = {}
        for key, col, caster in FIELDS:
            v = row[col - 1] if col - 1 < len(row) else None
            record[key] = _cell_to(v, caster)
        record["photo_path"] = photo_path_for_webui(record["photo_path"])
        martyrs.append(record)
    return {
        "generated_at": datetime.now().isoformat(timespec="seconds"),
        "channel": "AqmarTofan",
        "martyrs": martyrs,
    }


def main():
    payload = build_payload(DEFAULT_EXCEL)
    Path(DEFAULT_JSON).write_text(
        json.dumps(payload, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    print(f"Wrote {len(payload['martyrs'])} martyrs to {DEFAULT_JSON}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 3.4: Run tests (expect PASS — 4 tests)**

```powershell
.venv\Scripts\python.exe -m pytest tests/test_excel_to_json.py -v
```

Expected: 4 passed.

- [ ] **Step 3.5: Run the real conversion against `data/martyrs.xlsx`**

```powershell
.venv\Scripts\python.exe scripts\excel_to_json.py
```

Expected output: `Wrote N martyrs to data/martyrs.json` (N = current Excel row count, probably ~390).

- [ ] **Step 3.6: Spot-check the JSON shape**

```powershell
.venv\Scripts\python.exe -c "import json; d = json.load(open('data/martyrs.json', encoding='utf-8')); m = d['martyrs'][0]; import pprint; pprint.pp(m)"
```

Expected: a dict with all 14 fields present, `photo_path` starts with `../data/photos/`.

- [ ] **Step 3.7: Commit**

```powershell
git add scripts/excel_to_json.py tests/test_excel_to_json.py
git commit -m "feat: add excel_to_json.py with pytest tests

Reads data/martyrs.xlsx (read-only mode, so it's safe to run while
the scraper is mid-write) and emits data/martyrs.json shaped for the
webui's data-loader: photo paths rewritten relative to webui/ (../data/photos/N.jpg),
empty cells coerced to '' instead of null. 4/4 tests passing."
```

---

## Task 4: `webui/config.js`

**Files:**
- Modify: `webui/config.js`

- [ ] **Step 4.1: Write the config module**

Replace the entire contents of `webui/config.js`:

```javascript
// webui/config.js
// Static configuration for the SPA. Anyone with dev tools can see these
// values — the login is theater, not real security.

window.AQMAR_CONFIG = {
  appName: "أقمار الطوفان — قاعدة بيانات الشهداء",
  appNameEn: "AqmarTofan — Martyrs Database",
  channel: "AqmarTofan",

  // Data file paths, relative to webui/index.html
  martyrsJson:   "../data/martyrs.json",
  overridesJson: "../data/overrides.json",

  // Admin auth.
  // Default password is `aqmar2026`. To change it, compute the new hash with:
  //   python -c "import hashlib; print(hashlib.sha256(b'YOUR_NEW_PASSWORD').hexdigest())"
  // then paste below.
  adminUsername:     "admin",
  adminPasswordHash: "<PASTE-THE-HASH-FROM-TASK-2-HERE>",

  // Anti-bruteforce theater
  loginMaxAttempts:  3,
  loginLockSeconds:  30,

  // Filter defaults
  filterDefaultWindow: "1month",  // '1week' | '1month' | '2months' | 'custom'
  filterCustomDaysMin: 1,
  filterCustomDaysMax: 365,

  // localStorage keys
  storage: {
    auth:    "aqmar.auth",
    pending: "aqmar.pending_overrides",
  },
};
```

- [ ] **Step 4.2: Replace `<PASTE-THE-HASH-FROM-TASK-2-HERE>` with the actual hash**

Re-run the hash command:

```powershell
.venv\Scripts\python.exe -c "import hashlib; print(hashlib.sha256(b'aqmar2026').hexdigest())"
```

Copy the printed hex string (64 chars). Open `webui/config.js`, replace the placeholder with that string. Save.

- [ ] **Step 4.3: Verify the file is valid JS by loading it in Node**

```powershell
node -e "require('./webui/config.js'); console.log('ok')"
```

Wait — that will fail because the file uses `window`. Skip Node verification; just confirm the file looks right manually.

Better verification: open the file, confirm:
- `adminPasswordHash` is a 64-char hex string (no quotes inside, no whitespace)
- `martyrsJson` and `overridesJson` start with `"../data/"`

- [ ] **Step 4.4: Commit**

```powershell
git add webui/config.js
git commit -m "feat: webui config (paths, admin auth hash, filter defaults)

Single source of truth for the SPA's runtime constants. adminPasswordHash
is SHA-256 of 'aqmar2026' — change by re-hashing with the helper python
one-liner in the file's comments."
```

---

## Task 5: `webui/style.css`

**Files:**
- Modify: `webui/style.css`

- [ ] **Step 5.1: Write the custom CSS layer**

Replace the entire contents of `webui/style.css`:

```css
/* webui/style.css — custom layer on top of Tailwind */

/* Arabic-aware font stack with a sensible Latin fallback */
:root {
  font-family: "Cairo", "Tajawal", "Segoe UI", system-ui, -apple-system, sans-serif;
}

body {
  background: #0c1f0c;   /* dark green palette matching channel branding */
  color: #e8e8e8;
}

/* RTL by default — overridden per-element where Latin numbers/dates need LTR */
html { direction: rtl; }
.ltr { direction: ltr; }

/* Smooth grid scroll, prevent layout jank when modal opens */
html { scroll-behavior: smooth; }
body.modal-open { overflow: hidden; }

/* Card photo placeholder — gradient mimics our memorial palette */
.photo-placeholder {
  background: linear-gradient(135deg, #2d4a2d, #1a2e1a);
  display: flex; align-items: center; justify-content: center;
  color: #6f8a6f;
}

/* Edit-indicator dot (small ✏️ corner badge for overridden rows) */
.edit-dot {
  position: absolute; top: 0.4rem; left: 0.4rem;
  width: 1.4rem; height: 1.4rem;
  background: #fbbf24; color: #0c1f0c;
  border-radius: 999px;
  display: flex; align-items: center; justify-content: center;
  font-size: 0.75rem;
  box-shadow: 0 1px 4px rgba(0,0,0,0.4);
}

/* Custom scrollbar so the dark theme isn't broken by white scrollbars */
::-webkit-scrollbar { width: 10px; height: 10px; }
::-webkit-scrollbar-track { background: #0c1f0c; }
::-webkit-scrollbar-thumb { background: #2d4a2d; border-radius: 5px; }
::-webkit-scrollbar-thumb:hover { background: #3d5a3d; }
```

- [ ] **Step 5.2: Commit**

```powershell
git add webui/style.css
git commit -m "feat: webui custom styles

~30 lines of custom CSS on top of Tailwind: Arabic font stack with
Cairo/Tajawal preferred, dark-green page background matching the
channel palette, RTL default direction, photo placeholder gradient,
edit-indicator badge, themed scrollbars."
```

---

## Task 6: `webui/filter-logic.js` (TDD)

**Files:**
- Modify: `webui/filter-logic.js`
- Modify: `webui/tests.html`

- [ ] **Step 6.1: Write the test harness in `webui/tests.html` (initial version)**

Replace the entire contents of `webui/tests.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>AqmarTofan SPA — Tests</title>
  <style>
    body { font-family: monospace; background: #111; color: #eee; padding: 2rem; }
    .pass { color: #6ee06e; }
    .fail { color: #f55; }
    h2 { border-bottom: 1px solid #444; padding-bottom: 0.3rem; }
    pre { background: #222; padding: 0.5rem; border-radius: 4px; overflow: auto; }
  </style>
</head>
<body>
  <h1>SPA tests</h1>
  <div id="results"></div>

  <script>
    // Tiny test runner — pushes results into #results
    window.AQMAR_TESTS = [];
    window.test = (name, fn) => window.AQMAR_TESTS.push({ name, fn });

    function assertEq(actual, expected, msg) {
      const a = JSON.stringify(actual);
      const e = JSON.stringify(expected);
      if (a !== e) throw new Error(`${msg || "assert"}: expected ${e}, got ${a}`);
    }
    window.assertEq = assertEq;

    function runAll() {
      const out = document.getElementById("results");
      let pass = 0, fail = 0;
      for (const t of window.AQMAR_TESTS) {
        try {
          t.fn();
          out.innerHTML += `<div class="pass">✓ ${t.name}</div>`;
          pass++;
        } catch (e) {
          out.innerHTML += `<div class="fail">✗ ${t.name}<pre>${e.message}</pre></div>`;
          fail++;
        }
      }
      out.innerHTML += `<h2>${pass} passed, ${fail} failed</h2>`;
    }
    window.addEventListener("load", runAll);
  </script>

  <!-- Modules under test — order matters: dependencies first -->
  <script src="filter-logic.js"></script>

  <!-- Test files -->
  <script>
    // ===== filter-logic.js =====

    test("daysBetween returns 0 for same date", () => {
      assertEq(daysBetween("1985-06-15", "1985-06-15"), 0);
    });

    test("daysBetween returns positive when later", () => {
      assertEq(daysBetween("1985-06-15", "1985-06-20"), 5);
    });

    test("daysBetween returns negative when earlier", () => {
      assertEq(daysBetween("1985-06-15", "1985-06-10"), -5);
    });

    test("daysBetween handles cross-year", () => {
      assertEq(daysBetween("1985-12-31", "1986-01-01"), 1);
    });

    test("filterByProximity within 1month returns rows in window", () => {
      const rows = [
        { msg_id: 1, birth_date: "1985-06-10" }, // -5
        { msg_id: 2, birth_date: "1985-06-15" }, // 0
        { msg_id: 3, birth_date: "1985-07-20" }, // +35 (out)
        { msg_id: 4, birth_date: "" },           // (no birth date — skip)
        { msg_id: 5, birth_date: "1985-05-20" }, // -26
      ];
      const result = filterByProximity(rows, "1985-06-15", 30);
      assertEq(result.map(r => r.msg_id), [2, 1, 5]); // sorted by closeness
    });

    test("filterByProximity returns empty when userBirthdate is blank", () => {
      const rows = [{ msg_id: 1, birth_date: "1985-06-15" }];
      assertEq(filterByProximity(rows, "", 30), []);
    });

    test("filterByProximity attaches signed _delta_days", () => {
      const rows = [
        { msg_id: 1, birth_date: "1985-06-20" },
        { msg_id: 2, birth_date: "1985-06-10" },
      ];
      const result = filterByProximity(rows, "1985-06-15", 30);
      const deltas = result.map(r => r._delta_days);
      assertEq(deltas.sort(), [-5, 5]);
    });

    test("filterByProximity excludes rows beyond window", () => {
      const rows = [
        { msg_id: 1, birth_date: "1985-06-15" }, // 0
        { msg_id: 2, birth_date: "1985-08-15" }, // +61
      ];
      assertEq(filterByProximity(rows, "1985-06-15", 60).map(r => r.msg_id), [1]);
    });

    test("windowDaysFromMode resolves named modes", () => {
      assertEq(windowDaysFromMode("1week", 999), 7);
      assertEq(windowDaysFromMode("1month", 999), 30);
      assertEq(windowDaysFromMode("2months", 999), 60);
    });

    test("windowDaysFromMode clamps custom to [1,365]", () => {
      assertEq(windowDaysFromMode("custom", 0),    1);
      assertEq(windowDaysFromMode("custom", -10),  1);
      assertEq(windowDaysFromMode("custom", 999),  365);
      assertEq(windowDaysFromMode("custom", 45),   45);
    });

    test("windowDaysFromMode falls back to 30 for unknown mode", () => {
      assertEq(windowDaysFromMode("nonsense", 999), 30);
    });
  </script>
</body>
</html>
```

- [ ] **Step 6.2: Open `webui/tests.html` in a browser to confirm tests FAIL**

```powershell
cd webui
python -m http.server 8000
```

Then open `http://localhost:8000/tests.html`. Expected: all tests FAIL with `daysBetween is not defined` / `filterByProximity is not defined` / `windowDaysFromMode is not defined` (module file is empty).

Press Ctrl+C to stop the server.

- [ ] **Step 6.3: Implement `webui/filter-logic.js`**

Replace the entire contents of `webui/filter-logic.js`:

```javascript
// webui/filter-logic.js
// Birthdate-proximity filter math. Pure functions — no DOM, no fetch.

(function (global) {
  "use strict";

  const MS_PER_DAY = 86400000;

  function daysBetween(fromIsoDate, toIsoDate) {
    const a = new Date(fromIsoDate).getTime();
    const b = new Date(toIsoDate).getTime();
    return Math.round((b - a) / MS_PER_DAY);
  }

  function windowDaysFromMode(mode, customDays) {
    const named = { "1week": 7, "1month": 30, "2months": 60 };
    if (named[mode] !== undefined) return named[mode];
    if (mode === "custom") {
      const n = parseInt(customDays, 10);
      if (isNaN(n) || n < 1) return 1;
      if (n > 365) return 365;
      return n;
    }
    return 30;  // fallback for unknown mode
  }

  function filterByProximity(rows, userBirthdate, windowDays) {
    if (!userBirthdate) return [];
    return rows
      .filter(r => r.birth_date)
      .map(r => ({ ...r, _delta_days: daysBetween(userBirthdate, r.birth_date) }))
      .filter(r => Math.abs(r._delta_days) <= windowDays)
      .sort((a, b) => Math.abs(a._delta_days) - Math.abs(b._delta_days));
  }

  global.daysBetween = daysBetween;
  global.windowDaysFromMode = windowDaysFromMode;
  global.filterByProximity = filterByProximity;
})(window);
```

- [ ] **Step 6.4: Reload `tests.html` in the browser — confirm all PASS**

```powershell
cd webui
python -m http.server 8000
```

Open `http://localhost:8000/tests.html`. Expected: 11 green ✓ lines, "11 passed, 0 failed". Press Ctrl+C to stop.

- [ ] **Step 6.5: Commit**

```powershell
git add webui/filter-logic.js webui/tests.html
git commit -m "feat: filter-logic.js with browser-runnable tests (TDD)

Pure-function module for birthdate-proximity filtering used by the
public view. Three helpers:
  - daysBetween(a, b): signed integer days between two ISO dates
  - windowDaysFromMode(mode, customDays): named modes + custom clamp [1,365]
  - filterByProximity(rows, userBirthdate, windowDays): filter rows whose
    birth_date is within ±windowDays of userBirthdate, sorted by |delta|

tests.html: tiny in-page test runner that loads modules and prints
green/pass / red/fail. 11/11 tests passing."
```

---

## Task 7: `webui/data-loader.js` (TDD merge logic)

**Files:**
- Modify: `webui/data-loader.js`
- Modify: `webui/tests.html`

- [ ] **Step 7.1: Add data-loader to the test page**

Open `webui/tests.html`. Find the line `<script src="filter-logic.js"></script>` and add a new line **directly after** it:

```html
  <script src="data-loader.js"></script>
```

Then add a new test block **before the closing `</body>`** (after the existing filter-logic tests):

```html
  <script>
    // ===== data-loader.js =====

    test("mergeOverrides applies override fields on top of base", () => {
      const base = [
        { msg_id: 20, name: "A", birth_date: "1980-01-01" },
        { msg_id: 21, name: "B", birth_date: "1981-02-02" },
      ];
      const overrides = {
        "20": { birth_date: "1979-12-31", _manual_edit_at: "2026-05-13T10:00:00" },
      };
      const merged = mergeOverrides(base, overrides);
      assertEq(merged[0].birth_date, "1979-12-31");
      assertEq(merged[0].name, "A");                                // unchanged
      assertEq(merged[0]._overridden_fields, ["birth_date"]);
      assertEq(merged[1].birth_date, "1981-02-02");                 // untouched
      assertEq(merged[1]._overridden_fields, []);
    });

    test("mergeOverrides ignores meta fields when reporting overridden", () => {
      const base = [{ msg_id: 5, name: "X" }];
      const overrides = { "5": { name: "Y", _manual_edit_at: "2026-05-13" } };
      const merged = mergeOverrides(base, overrides);
      assertEq(merged[0].name, "Y");
      assertEq(merged[0]._overridden_fields, ["name"]);
    });

    test("mergeOverrides leaves rows alone when no override matches", () => {
      const base = [{ msg_id: 99, name: "Z" }];
      const merged = mergeOverrides(base, { "1": { name: "ignored" } });
      assertEq(merged[0].name, "Z");
      assertEq(merged[0]._overridden_fields, []);
    });

    test("mergeOverrides accepts numeric and string msg_id keys", () => {
      const base = [{ msg_id: 42, name: "old" }];
      const merged = mergeOverrides(base, { 42: { name: "new" } });
      assertEq(merged[0].name, "new");
    });

    test("mergeOverrides preserves row order from base", () => {
      const base = [{ msg_id: 3 }, { msg_id: 1 }, { msg_id: 2 }];
      const merged = mergeOverrides(base, {});
      assertEq(merged.map(r => r.msg_id), [3, 1, 2]);
    });
  </script>
```

- [ ] **Step 7.2: Reload `tests.html` — confirm new tests FAIL**

```powershell
cd webui
python -m http.server 8000
```

Open `http://localhost:8000/tests.html`. Expected: 5 new red ✗ lines for `mergeOverrides is not defined`.

- [ ] **Step 7.3: Implement `webui/data-loader.js`**

Replace the entire contents of `webui/data-loader.js`:

```javascript
// webui/data-loader.js
// Loads martyrs.json + overrides.json from disk, merges them.

(function (global) {
  "use strict";

  function mergeOverrides(baseMartyrs, overridesEdits) {
    // overridesEdits: { "20": { birth_date: "...", _manual_edit_at: "..." }, ... }
    const norm = {};
    for (const k of Object.keys(overridesEdits || {})) norm[String(k)] = overridesEdits[k];

    return baseMartyrs.map(row => {
      const ov = norm[String(row.msg_id)];
      if (!ov) return { ...row, _overridden_fields: [] };
      const merged = { ...row, ...ov };
      const overriddenFields = Object.keys(ov).filter(k => !k.startsWith("_"));
      merged._overridden_fields = overriddenFields;
      return merged;
    });
  }

  async function loadData(martyrsUrl, overridesUrl) {
    const [martyrsResp, overridesResp] = await Promise.all([
      fetch(martyrsUrl),
      fetch(overridesUrl).catch(() => null),  // overrides may not exist yet
    ]);

    if (!martyrsResp.ok) {
      throw new Error(`Cannot load ${martyrsUrl}: ${martyrsResp.status}. ` +
        `Run 'python scripts/excel_to_json.py' first.`);
    }
    const martyrsData = await martyrsResp.json();

    let overridesData = { version: 1, edits: {} };
    if (overridesResp && overridesResp.ok) {
      try {
        overridesData = await overridesResp.json();
      } catch (e) {
        console.warn("Invalid JSON in overrides — ignoring", e);
      }
    }
    return {
      generated_at: martyrsData.generated_at,
      channel: martyrsData.channel,
      martyrs: martyrsData.martyrs,
      overrides: overridesData.edits || {},
      allRows: mergeOverrides(martyrsData.martyrs, overridesData.edits || {}),
    };
  }

  global.mergeOverrides = mergeOverrides;
  global.loadData = loadData;
})(window);
```

- [ ] **Step 7.4: Reload `tests.html` — confirm ALL PASS**

Expected: 16 green ✓ lines (11 filter + 5 data-loader), "16 passed, 0 failed".

- [ ] **Step 7.5: Commit**

```powershell
git add webui/data-loader.js webui/tests.html
git commit -m "feat: data-loader.js merge logic with tests (TDD)

mergeOverrides(baseMartyrs, overridesEdits): pure function that takes
the canonical rows from martyrs.json and a partial diff map from
overrides.json (keyed by msg_id), returns merged rows. Each merged row
gets _overridden_fields listing which keys came from the override (used
by the UI to render an ✏️ badge). Meta keys (prefixed with _) are not
counted as overridden fields. 5/5 tests passing.

loadData(martyrsUrl, overridesUrl): async fetch helper. Tolerates a
missing overrides.json (treated as empty). Throws with a helpful
message if martyrs.json is missing (suggests running excel_to_json.py)."
```

---

## Task 8: `webui/admin-edit.js` (TDD)

**Files:**
- Modify: `webui/admin-edit.js`
- Modify: `webui/tests.html`

- [ ] **Step 8.1: Add admin-edit to the test page**

Open `webui/tests.html`. Find `<script src="data-loader.js"></script>` and add directly after:

```html
  <script src="admin-edit.js"></script>
```

Add a new test block before `</body>`:

```html
  <script>
    // ===== admin-edit.js =====

    test("buildEditDiff returns only changed fields", () => {
      const original = { name: "A", birth_date: "1980-01-01", city: "" };
      const edited =   { name: "B", birth_date: "1980-01-01", city: "غزة" };
      const diff = buildEditDiff(original, edited);
      assertEq(diff.name, "B");
      assertEq(diff.city, "غزة");
      assertEq(diff.birth_date, undefined);  // unchanged → not in diff
    });

    test("buildEditDiff returns empty object when nothing changed", () => {
      const original = { name: "A", birth_date: "1980-01-01" };
      const edited   = { name: "A", birth_date: "1980-01-01" };
      assertEq(buildEditDiff(original, edited), {});
    });

    test("buildEditDiff ignores fields starting with underscore", () => {
      const original = { name: "A", _delta_days: 5 };
      const edited =   { name: "A", _delta_days: 999 };
      assertEq(buildEditDiff(original, edited), {});
    });

    test("addEdit merges new diff with existing override + adds meta", () => {
      const existing = { "20": { name: "old override", _manual_edit_at: "2026-05-12" } };
      const diff = { birth_date: "1980-01-01" };
      const result = addEdit(existing, 20, diff, "2026-05-13T10:00:00", "admin");
      assertEq(result["20"].name, "old override");
      assertEq(result["20"].birth_date, "1980-01-01");
      assertEq(result["20"]._manual_edit_at, "2026-05-13T10:00:00");
      assertEq(result["20"]._editor, "admin");
    });

    test("addEdit creates a new override entry when none exists", () => {
      const result = addEdit({}, 99, { name: "fresh" }, "2026-05-13", "admin");
      assertEq(result["99"].name, "fresh");
      assertEq(result["99"]._manual_edit_at, "2026-05-13");
    });

    test("addEdit returns the same object reference unmodified when diff is empty", () => {
      const existing = { "20": { name: "old" } };
      const result = addEdit(existing, 20, {}, "2026-05-13", "admin");
      assertEq(result, existing);
    });

    test("buildExportPayload wraps edits with version", () => {
      const edits = { "20": { name: "X" } };
      const payload = buildExportPayload(edits);
      assertEq(payload.version, 1);
      assertEq(payload.edits, edits);
    });
  </script>
```

- [ ] **Step 8.2: Reload `tests.html` — confirm new tests FAIL**

Expected: 7 new red ✗ for undefined `buildEditDiff` / `addEdit` / `buildExportPayload`.

- [ ] **Step 8.3: Implement `webui/admin-edit.js`**

Replace the entire contents of `webui/admin-edit.js`:

```javascript
// webui/admin-edit.js
// Admin edit logic: diff computation, override accumulation, export.

(function (global) {
  "use strict";

  function buildEditDiff(original, edited) {
    const diff = {};
    const keys = new Set([...Object.keys(original), ...Object.keys(edited)]);
    for (const k of keys) {
      if (k.startsWith("_")) continue;     // skip internal/meta
      if (original[k] !== edited[k]) diff[k] = edited[k];
    }
    return diff;
  }

  function addEdit(existingOverrides, msgId, diff, timestampIso, editor) {
    if (Object.keys(diff).length === 0) return existingOverrides;
    const key = String(msgId);
    const prior = existingOverrides[key] || {};
    return {
      ...existingOverrides,
      [key]: {
        ...prior,
        ...diff,
        _manual_edit_at: timestampIso,
        _editor: editor || "admin",
      },
    };
  }

  function buildExportPayload(overridesEdits) {
    return { version: 1, edits: overridesEdits };
  }

  function downloadOverridesJson(overridesEdits) {
    const payload = buildExportPayload(overridesEdits);
    const text = JSON.stringify(payload, null, 2);
    const blob = new Blob([text], { type: "application/json;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = "overrides.json";
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  }

  global.buildEditDiff = buildEditDiff;
  global.addEdit = addEdit;
  global.buildExportPayload = buildExportPayload;
  global.downloadOverridesJson = downloadOverridesJson;
})(window);
```

- [ ] **Step 8.4: Reload `tests.html` — confirm ALL PASS**

Expected: 23 green ✓ lines, "23 passed, 0 failed".

- [ ] **Step 8.5: Commit**

```powershell
git add webui/admin-edit.js webui/tests.html
git commit -m "feat: admin-edit.js with tests (TDD)

Pure-logic helpers for the admin edit flow:
  - buildEditDiff(original, edited): returns only changed non-meta fields
  - addEdit(existing, msgId, diff, ts, editor): merges a diff into the
    overrides map for a given msg_id, stamps with timestamp + editor
  - buildExportPayload(edits): wraps with {version:1, edits} shape
  - downloadOverridesJson(edits): browser-only — creates a Blob and
    triggers a download of overrides.json (not unit-tested, smoke-checked
    later via the index.html UI)

7/7 unit tests passing."
```

---

## Task 9: `webui/app.js` (Alpine root component)

**Files:**
- Modify: `webui/app.js`

This is the glue layer. Not unit-tested in isolation — covered by manual end-to-end in Task 11.

- [ ] **Step 9.1: Write the Alpine factory**

Replace the entire contents of `webui/app.js`:

```javascript
// webui/app.js
// Alpine.js root component for the AqmarTofan SPA.

window.app = function () {
  return {
    // === auth state ===
    loggedIn: localStorage.getItem(AQMAR_CONFIG.storage.auth) === "yes",
    loginUser: "",
    loginPass: "",
    loginError: "",
    showLoginModal: false,
    failedAttempts: 0,
    lockedUntil: 0,

    // === data state ===
    martyrs: [],
    overrides: {},
    pendingOverrides: {},                          // unexported localStorage edits
    allRows: [],
    isLoading: true,
    loadError: "",

    // === view state ===
    view: "public",                                // 'public' | 'admin'
    selectedRow: null,                             // photo zoom modal
    editingMsgId: null,                            // admin edit modal target
    editForm: {},                                  // bound to inputs in the modal

    // === public filter state ===
    userBirthdate: "",
    windowMode: AQMAR_CONFIG.filterDefaultWindow,
    customDays: 30,
    filteredResults: [],
    customDaysError: "",

    // === computed-ish helpers (called from templates) ===
    get pendingEditCount() {
      return Object.keys(this.pendingOverrides).length;
    },
    get effectiveOverrides() {
      // Saved overrides + in-progress pending overrides (pending wins).
      const out = { ...this.overrides };
      for (const k of Object.keys(this.pendingOverrides)) {
        out[k] = { ...(out[k] || {}), ...this.pendingOverrides[k] };
      }
      return out;
    },

    // === lifecycle ===
    async init() {
      try {
        const data = await loadData(
          AQMAR_CONFIG.martyrsJson,
          AQMAR_CONFIG.overridesJson,
        );
        this.martyrs = data.martyrs;
        this.overrides = data.overrides;
        const pending = localStorage.getItem(AQMAR_CONFIG.storage.pending);
        if (pending) {
          try { this.pendingOverrides = JSON.parse(pending); }
          catch (e) { console.warn("Bad pendingOverrides in localStorage", e); }
        }
        this.refreshAllRows();
      } catch (e) {
        this.loadError = e.message;
      } finally {
        this.isLoading = false;
      }
    },

    refreshAllRows() {
      this.allRows = mergeOverrides(this.martyrs, this.effectiveOverrides);
      this.applyFilter();
    },

    // === filter ===
    applyFilter() {
      this.customDaysError = "";
      if (this.windowMode === "custom") {
        const n = parseInt(this.customDays, 10);
        if (isNaN(n) || n < AQMAR_CONFIG.filterCustomDaysMin || n > AQMAR_CONFIG.filterCustomDaysMax) {
          this.customDaysError = `${AQMAR_CONFIG.filterCustomDaysMin} - ${AQMAR_CONFIG.filterCustomDaysMax}`;
        }
      }
      const days = windowDaysFromMode(this.windowMode, this.customDays);
      this.filteredResults = filterByProximity(this.allRows, this.userBirthdate, days);
    },

    // === modals ===
    openPhotoModal(row) {
      this.selectedRow = row;
      document.body.classList.add("modal-open");
    },
    closePhotoModal() {
      this.selectedRow = null;
      document.body.classList.remove("modal-open");
    },
    openEditModal(row) {
      this.editingMsgId = row.msg_id;
      const editableFields = [
        "name", "birth_date", "martyrdom_date", "city",
        "military_rank", "weapon", "battalion", "brigade",
      ];
      this.editForm = {};
      for (const f of editableFields) this.editForm[f] = row[f] || "";
      document.body.classList.add("modal-open");
    },
    closeEditModal() {
      this.editingMsgId = null;
      this.editForm = {};
      document.body.classList.remove("modal-open");
    },

    // === admin actions ===
    saveEdit() {
      const original = this.allRows.find(r => r.msg_id === this.editingMsgId);
      if (!original) return;
      const diff = buildEditDiff(original, this.editForm);
      if (Object.keys(diff).length === 0) {
        this.closeEditModal();
        return;
      }
      this.pendingOverrides = addEdit(
        this.pendingOverrides,
        this.editingMsgId,
        diff,
        new Date().toISOString(),
        "admin",
      );
      localStorage.setItem(
        AQMAR_CONFIG.storage.pending,
        JSON.stringify(this.pendingOverrides),
      );
      this.refreshAllRows();
      this.closeEditModal();
    },

    exportOverrides() {
      const merged = this.effectiveOverrides;
      downloadOverridesJson(merged);
      // Optimistically move pending into overrides since the admin is about
      // to save the file — UI feels responsive even before reload.
      this.overrides = { ...merged };
      this.pendingOverrides = {};
      localStorage.removeItem(AQMAR_CONFIG.storage.pending);
    },

    // === login ===
    async login() {
      const now = Date.now();
      if (now < this.lockedUntil) {
        const remain = Math.ceil((this.lockedUntil - now) / 1000);
        this.loginError = `قُفل الدخول. حاول بعد ${remain} ثانية.`;
        return;
      }
      if (this.loginUser !== AQMAR_CONFIG.adminUsername) {
        this._recordFailedAttempt();
        return;
      }
      const buf = await crypto.subtle.digest(
        "SHA-256",
        new TextEncoder().encode(this.loginPass),
      );
      const hex = Array.from(new Uint8Array(buf))
        .map(b => b.toString(16).padStart(2, "0"))
        .join("");
      if (hex !== AQMAR_CONFIG.adminPasswordHash) {
        this._recordFailedAttempt();
        return;
      }
      // success
      this.failedAttempts = 0;
      this.lockedUntil = 0;
      this.loginError = "";
      localStorage.setItem(AQMAR_CONFIG.storage.auth, "yes");
      this.loggedIn = true;
      this.view = "admin";
      this.showLoginModal = false;
      this.loginPass = "";
    },

    _recordFailedAttempt() {
      this.failedAttempts++;
      if (this.failedAttempts >= AQMAR_CONFIG.loginMaxAttempts) {
        this.lockedUntil = Date.now() + AQMAR_CONFIG.loginLockSeconds * 1000;
        this.loginError = `قُفل الدخول بعد ${AQMAR_CONFIG.loginMaxAttempts} محاولات خاطئة.`;
        this.failedAttempts = 0;
      } else {
        this.loginError = "خطأ في اسم المستخدم أو كلمة المرور";
      }
    },

    logout() {
      localStorage.removeItem(AQMAR_CONFIG.storage.auth);
      this.loggedIn = false;
      this.view = "public";
    },
  };
};
```

- [ ] **Step 9.2: Commit**

```powershell
git add webui/app.js
git commit -m "feat: webui/app.js — Alpine.js root component

Glue layer that consumes filter-logic.js + data-loader.js + admin-edit.js.
Manages: auth state (with anti-bruteforce lockout), data load + merge,
view toggle (public/admin), photo/edit modals, filter inputs, admin
edit form bindings, export action. Verified end-to-end in Task 11."
```

---

## Task 10: `webui/index.html`

**Files:**
- Modify: `webui/index.html`

- [ ] **Step 10.1: Write the markup**

Replace the entire contents of `webui/index.html`:

```html
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>أقمار الطوفان</title>

  <!-- Tailwind via Play CDN (no build) -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: { extend: { colors: {
        bgdark: "#0c1f0c", bgcard: "#142914", brand: "#fbbf24", border: "#2d4a2d",
      }}}
    };
  </script>

  <!-- Alpine.js (defer per docs) -->
  <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>

  <!-- App modules — load order matters -->
  <script src="config.js"></script>
  <script src="filter-logic.js"></script>
  <script src="data-loader.js"></script>
  <script src="admin-edit.js"></script>
  <script src="app.js"></script>

  <link rel="stylesheet" href="style.css">
</head>
<body x-data="app()" x-init="init()" class="min-h-screen bg-bgdark text-white">

<!-- Loading / error banners -->
<div x-show="isLoading" class="text-center py-12 text-lg">جاري التحميل...</div>
<div x-show="loadError" class="bg-red-900 text-red-100 p-4 m-4 rounded" x-text="loadError"></div>

<!-- Header -->
<header class="bg-bgcard border-b border-border sticky top-0 z-10 px-4 py-3">
  <div class="max-w-7xl mx-auto flex flex-wrap items-center gap-3">
    <h1 class="text-xl font-bold flex-shrink-0">
      <span x-text="AQMAR_CONFIG.appName"></span>
    </h1>

    <!-- View toggle (admin only) -->
    <div x-show="loggedIn" class="flex gap-2 mr-auto">
      <button @click="view='public'" :class="view==='public' ? 'bg-brand text-bgdark' : 'bg-bgdark border border-border'"
        class="px-3 py-1 rounded text-sm">عام</button>
      <button @click="view='admin'" :class="view==='admin' ? 'bg-brand text-bgdark' : 'bg-bgdark border border-border'"
        class="px-3 py-1 rounded text-sm">إدارة</button>
    </div>

    <!-- Login/Logout (auth controls) -->
    <button x-show="!loggedIn" @click="showLoginModal=true"
      class="mr-auto px-3 py-1 bg-brand text-bgdark rounded font-bold text-sm">دخول الإدارة</button>
    <button x-show="loggedIn" @click="logout()"
      class="px-3 py-1 bg-bgdark border border-border rounded text-sm">خروج</button>
  </div>

  <!-- Filter bar (public view) -->
  <div x-show="view==='public' && !isLoading" class="max-w-7xl mx-auto mt-3 flex flex-wrap items-center gap-3">
    <label class="text-sm">تاريخ ميلادي:
      <input type="date" x-model="userBirthdate" @change="applyFilter()"
        class="bg-bgdark border border-border rounded px-2 py-1 ltr text-sm">
    </label>
    <label class="text-sm">النافذة:
      <select x-model="windowMode" @change="applyFilter()"
        class="bg-bgdark border border-border rounded px-2 py-1 text-sm">
        <option value="1week">أسبوع (±7 يوم)</option>
        <option value="1month">شهر (±30 يوم)</option>
        <option value="2months">شهرين (±60 يوم)</option>
        <option value="custom">مخصّص</option>
      </select>
    </label>
    <label x-show="windowMode==='custom'" class="text-sm">عدد الأيام:
      <input type="number" min="1" max="365" x-model="customDays" @input="applyFilter()"
        class="bg-bgdark border border-border rounded px-2 py-1 w-20 ltr text-sm">
      <span x-show="customDaysError" class="text-red-400 text-xs" x-text="customDaysError"></span>
    </label>
    <span class="text-brand text-sm mr-auto" x-text="`${filteredResults.length} شهيد`"></span>
  </div>

  <!-- Admin tip (admin view) -->
  <div x-show="view==='admin' && !isLoading" class="max-w-7xl mx-auto mt-3 flex flex-wrap items-center gap-3">
    <span class="text-sm">عدد الشهداء: <span x-text="allRows.length" class="text-brand"></span></span>
    <button @click="exportOverrides()" x-show="pendingEditCount > 0"
      class="mr-auto bg-brand text-bgdark px-3 py-1 rounded font-bold text-sm">
      💾 تصدير <span x-text="pendingEditCount"></span> تعديل
    </button>
  </div>
</header>

<!-- ============ PUBLIC VIEW ============ -->
<main x-show="view==='public' && !isLoading" class="max-w-7xl mx-auto p-4">
  <!-- Empty state -->
  <div x-show="!userBirthdate" class="text-center py-16 text-gray-400">
    أدخل تاريخ ميلادك في الأعلى لعرض الشهداء بأعمار قريبة.
  </div>
  <div x-show="userBirthdate && filteredResults.length === 0" class="text-center py-16 text-gray-400">
    لا يوجد شهداء بتاريخ ميلاد قريب من إدخالك. جرّب نافذة أوسع.
  </div>

  <!-- Grid -->
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    <template x-for="row in filteredResults" :key="row.msg_id">
      <div class="bg-bgcard border border-border rounded-lg overflow-hidden cursor-pointer hover:border-brand transition-colors relative"
           @click="openPhotoModal(row)">
        <div x-show="row._overridden_fields && row._overridden_fields.length > 0" class="edit-dot" title="تعديل يدوي">✏️</div>
        <div class="aspect-[4/5] bg-gradient-to-br from-bgcard to-bgdark">
          <img x-show="row.photo_path" :src="row.photo_path" :alt="row.name" loading="lazy"
            class="w-full h-full object-cover" @error="$el.style.display='none'; $el.nextElementSibling.style.display='flex'">
          <div class="photo-placeholder w-full h-full" style="display:none">صورة غير متوفرة</div>
        </div>
        <div class="p-3 text-right">
          <div class="font-bold text-sm" x-text="row.name"></div>
          <div class="text-xs text-brand mt-1 ltr text-left" x-text="`${row.birth_date || '—'} → ${row.martyrdom_date || '—'}`"></div>
          <div class="inline-block bg-brand text-bgdark text-xs font-bold px-2 py-0.5 rounded mt-2 ltr"
            x-text="`±${Math.abs(row._delta_days)} يوم`"></div>
        </div>
      </div>
    </template>
  </div>
</main>

<!-- ============ ADMIN VIEW ============ -->
<main x-show="view==='admin' && !isLoading && loggedIn" class="max-w-7xl mx-auto p-4">
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    <template x-for="row in allRows" :key="row.msg_id">
      <div class="bg-bgcard border border-border rounded-lg overflow-hidden relative">
        <div x-show="row._overridden_fields && row._overridden_fields.length > 0" class="edit-dot" title="تعديل يدوي">✏️</div>
        <div class="aspect-[4/5] bg-gradient-to-br from-bgcard to-bgdark cursor-pointer"
             @click="openPhotoModal(row)">
          <img x-show="row.photo_path" :src="row.photo_path" :alt="row.name" loading="lazy"
            class="w-full h-full object-cover" @error="$el.style.display='none'; $el.nextElementSibling.style.display='flex'">
          <div class="photo-placeholder w-full h-full" style="display:none">صورة غير متوفرة</div>
        </div>
        <div class="p-3 text-right">
          <div class="font-bold text-sm" x-text="row.name"></div>
          <div class="text-xs text-gray-400 mt-1">معرّف: <span x-text="row.msg_id"></span></div>
          <div class="text-xs text-brand mt-1 ltr text-left" x-text="`${row.birth_date || '—'} → ${row.martyrdom_date || '—'}`"></div>
          <div class="flex gap-2 mt-2">
            <button @click="openEditModal(row)" class="text-xs bg-brand text-bgdark px-2 py-1 rounded">✏️ تحرير</button>
            <a :href="row.message_link" target="_blank" class="text-xs bg-bgdark border border-border px-2 py-1 rounded">↗ تيليجرام</a>
          </div>
        </div>
      </div>
    </template>
  </div>
</main>

<!-- ============ PHOTO ZOOM MODAL ============ -->
<div x-show="selectedRow" @click.self="closePhotoModal()" x-cloak
     class="fixed inset-0 bg-black/80 z-20 flex items-center justify-center p-4">
  <div class="bg-bgcard border border-border rounded-lg overflow-hidden max-w-2xl w-full max-h-[90vh] flex flex-col">
    <div class="flex justify-between items-center p-3 border-b border-border">
      <h3 class="font-bold" x-text="selectedRow?.name"></h3>
      <button @click="closePhotoModal()" class="text-xl">✕</button>
    </div>
    <img x-show="selectedRow?.photo_path" :src="selectedRow?.photo_path" :alt="selectedRow?.name"
         class="w-full max-h-[60vh] object-contain bg-bgdark">
    <div class="p-3 text-sm space-y-1">
      <div>تاريخ الميلاد: <span class="text-brand ltr" x-text="selectedRow?.birth_date || '—'"></span></div>
      <div>تاريخ الاستشهاد: <span class="text-brand ltr" x-text="selectedRow?.martyrdom_date || '—'"></span></div>
      <div>الرتبة: <span x-text="selectedRow?.military_rank || '—'"></span></div>
      <div>الكتيبة: <span x-text="selectedRow?.battalion || '—'"></span></div>
      <div>اللواء: <span x-text="selectedRow?.brigade || '—'"></span></div>
      <a :href="selectedRow?.message_link" target="_blank" class="text-brand text-xs underline">عرض في تيليجرام ↗</a>
    </div>
  </div>
</div>

<!-- ============ ADMIN EDIT MODAL ============ -->
<div x-show="editingMsgId" @click.self="closeEditModal()" x-cloak
     class="fixed inset-0 bg-black/80 z-20 flex items-center justify-center p-4">
  <div class="bg-bgcard border border-border rounded-lg overflow-hidden max-w-md w-full max-h-[90vh] flex flex-col">
    <div class="flex justify-between items-center p-3 border-b border-border">
      <h3 class="font-bold">تحرير الشهيد (معرّف <span x-text="editingMsgId"></span>)</h3>
      <button @click="closeEditModal()" class="text-xl">✕</button>
    </div>
    <form @submit.prevent="saveEdit()" class="p-3 space-y-2 overflow-y-auto text-sm">
      <template x-for="field in ['name','birth_date','martyrdom_date','city','military_rank','weapon','battalion','brigade']">
        <label class="block">
          <span class="block text-xs text-gray-300" x-text="field"></span>
          <input :type="field.includes('date') ? 'date' : 'text'" x-model="editForm[field]"
            class="w-full bg-bgdark border border-border rounded px-2 py-1 ltr">
        </label>
      </template>
      <div class="flex gap-2 pt-2">
        <button type="submit" class="bg-brand text-bgdark px-3 py-1 rounded font-bold flex-1">حفظ التعديل</button>
        <button type="button" @click="closeEditModal()" class="bg-bgdark border border-border px-3 py-1 rounded">إلغاء</button>
      </div>
    </form>
  </div>
</div>

<!-- ============ LOGIN MODAL ============ -->
<div x-show="showLoginModal" @click.self="showLoginModal=false" x-cloak
     class="fixed inset-0 bg-black/80 z-20 flex items-center justify-center p-4">
  <div class="bg-bgcard border border-border rounded-lg p-5 max-w-sm w-full">
    <h3 class="font-bold mb-3">دخول الإدارة</h3>
    <form @submit.prevent="login()" class="space-y-2 text-sm">
      <input type="text" x-model="loginUser" placeholder="اسم المستخدم"
        class="w-full bg-bgdark border border-border rounded px-2 py-2 ltr">
      <input type="password" x-model="loginPass" placeholder="كلمة المرور"
        class="w-full bg-bgdark border border-border rounded px-2 py-2 ltr">
      <div x-show="loginError" class="text-red-400 text-xs" x-text="loginError"></div>
      <div class="flex gap-2 pt-2">
        <button type="submit" class="bg-brand text-bgdark px-3 py-2 rounded font-bold flex-1">دخول</button>
        <button type="button" @click="showLoginModal=false" class="bg-bgdark border border-border px-3 py-2 rounded">إلغاء</button>
      </div>
    </form>
  </div>
</div>

</body>
</html>
```

- [ ] **Step 10.2: Open in browser to verify it loads**

```powershell
cd webui
python -m http.server 8000
```

Open `http://localhost:8000`. Expected:
- Page loads with dark green theme
- "جاري التحميل..." flashes, then the empty state shows ("أدخل تاريخ ميلادك في الأعلى...")
- Header has the title, "دخول الإدارة" button
- Filter bar shows date input, dropdown, no results yet
- No console errors

Press Ctrl+C to stop.

- [ ] **Step 10.3: Commit**

```powershell
git add webui/index.html
git commit -m "feat: webui/index.html — markup, Tailwind, Alpine bindings

The full SPA in one HTML file (~150 lines): sticky header with title +
auth controls + filter bar, public grid view, admin grid view with
edit/Telegram-link buttons, three modals (photo zoom, edit form,
login). Tailwind Play CDN + Alpine.js CDN — no build step.

Bindings reference the methods defined in app.js (init, applyFilter,
openPhotoModal, openEditModal, saveEdit, exportOverrides, login, logout).
RTL layout with ltr utility class for Latin-only date inputs.

Manual smoke verified: page renders, empty state shows correctly."
```

---

## Task 11: End-to-end manual verification

**Files:** none modified (just running and clicking)

This task is a checklist you walk through in a real browser. Mark each item as you verify it.

- [ ] **Step 11.1: Generate fresh JSON, start the server**

```powershell
.venv\Scripts\python.exe scripts\excel_to_json.py
cd webui
python -m http.server 8000
```

Open `http://localhost:8000`.

- [ ] **Step 11.2: Public view — filter by birthdate**

1. Enter a birthdate (e.g. `1985-06-15`) in the filter bar.
2. Select "شهر (±30 يوم)" (1 month).
3. Expected: the grid shows martyrs born within ±30 days, sorted by closest first. Result count matches.
4. Click any card. Expected: photo zoom modal opens with full info.
5. Close the modal. Try "1 week" then "2 months" — count updates.
6. Try "مخصّص" (custom) — enter `90` → grid widens; enter `999` → clamps with a hint.

- [ ] **Step 11.3: Photos load**

1. In the grid, confirm at least the first few photos render (not "صورة غير متوفرة").
2. For one row with no photo on disk (or where `photo_path` is empty), the placeholder shows.

- [ ] **Step 11.4: Login flow**

1. Click "دخول الإدارة" — login modal opens.
2. Type wrong username → "خطأ في اسم المستخدم أو كلمة المرور".
3. Type `admin` + wrong password → same error. Two more wrong attempts → lock message with "30 ثانية".
4. Wait or refresh, then enter `admin` / `aqmar2026` (the default password) → modal closes, view switches to admin.

- [ ] **Step 11.5: Admin edit + export**

1. In admin view, find a row you know has bad data (e.g. one with `extraction_status` = `partial_birth`).
2. Click "✏️ تحرير" → edit modal opens with all fields prefilled.
3. Change the birth_date to a known-correct value. Click "حفظ التعديل".
4. Expected: modal closes, the card now has an ✏️ corner badge, the new birth_date is visible. Header shows "💾 تصدير 1 تعديل".
5. Click "💾 تصدير 1 تعديل". Browser downloads `overrides.json`. Open it — confirm the edited fields are present with `_manual_edit_at` and `_editor`.

- [ ] **Step 11.6: Persistence**

1. Save the downloaded `overrides.json` into `D:\Repo\01-Khamis-Projects\AQMAR\data\overrides.json` (overwriting the empty seed).
2. Hard reload the browser (Ctrl+F5).
3. Expected: the ✏️ badge on the edited row is still there. Header no longer shows the "💾 تصدير" button (no pending edits).

- [ ] **Step 11.7: Logout + public re-entry**

1. Click "خروج". View returns to public.
2. Hard reload — still in public view (auth cleared).
3. Filter still works.

- [ ] **Step 11.8: Browser tests still pass**

Open `http://localhost:8000/tests.html`. Expected: "23 passed, 0 failed".

- [ ] **Step 11.9: If everything works, commit a no-op marker (or skip)**

```powershell
git status
# If clean, no commit needed. Otherwise commit any small fixes you made.
```

---

## Task 12: Update README

**Files:**
- Modify: `README.md`

- [ ] **Step 12.1: Expand the Web UI section**

Open `README.md`. Find the "## Web UI" section added in Task 1. Replace it with:

```markdown
## Web UI

A static Alpine.js + Tailwind SPA that browses and edits the martyr data,
with two views:

- **Public:** enter your birthdate + a window (1 week / 1 month / 2 months /
  custom days) → see martyrs born within ±N days of you, sorted by closeness.
- **Admin** (login required): same grid but each card has an "✏️ تحرير"
  button to fix wrong / missing fields. Edits accumulate in `localStorage`;
  click "💾 تصدير" to download `overrides.json`, then save it into
  `data/overrides.json`. Edits survive pipeline re-runs.

### Run locally

```powershell
.venv\Scripts\activate
python scripts\excel_to_json.py    # generate data/martyrs.json from xlsx
cd webui
python -m http.server 8000
start http://localhost:8000
```

### Default admin login

- Username: `admin`
- Password: `aqmar2026`

To change the password, edit `webui/config.js` and replace `adminPasswordHash`
with the SHA-256 hex of your new password:

```powershell
.venv\Scripts\python.exe -c "import hashlib; print(hashlib.sha256(b'YOUR_NEW_PASSWORD').hexdigest())"
```

### Tests

Open `http://localhost:8000/tests.html` while the server is running.
Tests run on page load — all 23 should pass.

### Daily workflow

```powershell
.venv\Scripts\activate
python scripts\phase3_daily.py     # fetch new posts from the channel
python scripts\excel_to_json.py    # regenerate JSON for the SPA
# Reload the browser tab — the SPA picks up new rows.
```

### Future hosting

`webui/` + `data/martyrs.json` + `data/overrides.json` + `data/photos/`
can be copied to any static host (GitHub Pages, Netlify, Cloudflare Pages).
The relative `../data/photos/N.jpg` paths preserve the on-disk structure.
```

- [ ] **Step 12.2: Commit**

```powershell
git add README.md
git commit -m "docs: README — full Web UI section

How to run locally, default admin credentials, password-change recipe,
where to find browser tests, daily-workflow command sequence, future
static-hosting notes."
```

---

## Self-review (performed after writing this plan)

| Check | Result |
|---|---|
| **Spec coverage** | All 15 spec sections map to tasks: §3 architecture→T9-T10, §4 stack→T1+T10, §5 layout→T1+T3, §6 data shapes→T1+T3, §7 component→T9, §8 filter math→T6, §9 edit flow→T8+T9, §10 login→T9, §11 UI→T5+T10, §12 errors→T6+T10+T11.6, §13 ops→T12, §14 acceptance→T11. |
| **Placeholder scan** | `<PASTE-THE-HASH-FROM-TASK-2-HERE>` in Task 4 is intentional and explicitly replaced in step 4.2. The illustrative SHA hex value in Task 2 (step 2.1) is marked as illustrative — engineer records what they actually computed. No `TBD`/`TODO`/`fill in` left elsewhere. |
| **Type consistency** | `mergeOverrides(baseMartyrs, overridesEdits)`, `daysBetween(fromIsoDate, toIsoDate)`, `addEdit(existing, msgId, diff, timestampIso, editor)` — all signatures consistent across tests and implementations. `AQMAR_CONFIG.storage.auth` / `AQMAR_CONFIG.storage.pending` consistently referenced. Excel column order in test fixture (Task 3 step 3.1) matches FIELDS array order in implementation (step 3.3). |
| **Git** | Each task commits at the end. User pre-approved auto-commits via "yes-and-auto". |
