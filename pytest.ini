[pytest]
# Only collect the real test suite. scripts/ contains files named like tests
# (e.g. test_partial_video.py) that are actually ad-hoc PoC scripts — without
# this, pytest would import and run them (incl. live network calls) on collect.
testpaths = tests
